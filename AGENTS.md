# قواعد وضوابط مشروع حساباتي | Hisabati
## Project Governance & Engineering Hardening

هذا الملف يحتوي على القواعد والضوابط الإلزامية المستمرة للمشروع (القواعد 1 إلى 69) لضمان حوكمة الكود والنزاهة المحاسبية والهندسية.

---

### أولاً: القواعد الأساسية للمشروع (1 - 42)
1. **لا تعيد بناء المشروع من الصفر**: Extend, Don't Destroy. فحص الكود الحالي قبل أي تعديل، تنفيذ أقل تغيير آمن، والحفاظ على الميزات القائمة.
2. **لا تفترض وجود شيء غير موجود**: التحقق من الملفات، الـComponents، الـServices، قاعدة البيانات، والـRoutes قبل الادعاء بتنفيذ الميزة.
3. **الالتزام بالمراحل المحددة (Phased Development)**: عدم القفز إلى مراحل مستقبلية وتنفيذ نطاق المرحلة الحالية فقط.
4. **Mobile First إلزامي**: التصميم موجه أولاً لشاشات الهواتف مع منع الـHorizontal overflow وضمان التجاوب الكامل.
5. **Arabic RTL First**: العربية هي اللغة الأساسية، والتصميم RTL افتراضياً باستخدام logical properties (`start/end`, `ms/me`, `ps/pe`).
6. **عدم تغيير النصوص التجارية والقائمة** إلا بطلب صريح.
7. **الحفاظ على الهوية البصرية** (الألوان الصلبة، الحدود، والخطوط).
8. **ممنوع Gradients**: استخدام ألوان صلبة (Solid Colors) بمظهر نظيف ومهني.
9. **استخدام Lucide Icons** وتجنب الـEmoji داخل واجهة التطبيق.
10. **عدم إضافة Dependencies بدون ضرورة قصوى**.
11. **Database Architecture**: فصل الطبقات (UI -> Hooks/Stores -> Services -> Repositories -> Database).
12. **Offline First**: قاعدة البيانات المحلية هي مصدر التشغيل الأساسي دون اشتراط الإنترنت.
13. **Performance**: تجنب الـRe-renders والـQueries المتكررة واستخدام الميموري بكفاءة.
14. **منع الحلول الوهمية**: منع `setTimeout` لعلاج مشاكل الـDB، وتجنب fake loading أو mock success.
15. **Error Handling**: معالجة الأخطاء برسائل عربية واضحة للمستخدم دون إظهار stack traces أو بيانات حساسة.
16. **البيانات المالية**: الاعتماد الصارم على `minor units / integer` ومنع أخطاء floating-point الحسابية.
17. **Transaction Safety**: العمليات المالية ذرية (Atomic) وقابلة للاسترجاع (Rollback/Recovery).
18. **Auditability**: تتبع التواريخ والعمليات (`createdAt`, `updatedAt`, `createdBy`, `deletedAt`).
19. **Security**: عدم تضمين أي أسرار أو مفاتيح في كود الواجهة الأمامية.
20. **AI Safety**: الذكاء الاصطناعي ليس مصدراً للحقيقة المالية، وقواعد العمل + قاعدة البيانات هي المرجع.
21. **AI Gateway**: عدم استدعاء AI مباشرة من العميل.
22. **Form Validation**: تدقيق صارم للمبالغ والتواريخ والحسابات قبل الحفظ.
23. **UX مبسط**: أقل عدد من الخطوات لإنجاز العمليات المحاسبية الأساسية.
24. **Touch Targets**: مساحات لمس لا تقل عن 44px × 44px للعناصر التفاعلية.
25. **Navigation**: الحفاظ على هيكل التنقل القائم دون تعارض.
26. **Modularity**: تقسيم المكونات بشكل منطقي وإعادة استخدام المفيد منها.
27. **Naming Conventions**: تسميات واضحة ومقروءة (PascalCase, camelCase).
28. **Strict TypeScript**: تجنب `any` أو `as any` لعلاج الأخطاء.
29. **Build Quality Gate**: التحقق من TypeScript، Build، الاختبارات، وLint.
30. **التحقق التشغيلي**: التأكد من Runtime وليس فقط ظهور الصفحة.
31. **معالجة الأخطاء البرمجية (Bugs)**: تتبع جذر المشكلة (Root Cause) وتجنب Regression.
32. **عدم حذف الكود القديم** إلا بعد التحقق من عدم وجود مراجع أو تبعيات مستقبلية.
33. **حماية Schema**: ترقية مدروسة ومتوافقة مع البيانات الحالية.
34. **الحفاظ على البيانات**: منع مسح قاعدة بيانات المستخدم.
35. **عزل البيانات التجريبية** عن البيئة التشغيلية.
36. **تحليل التأثير قبل التعديل الكبير**.
37. **طلب توضيح عند الغموض الذي يمس الأمان أو البيانات**.
38. **الثبات على الـStack**: React, TypeScript, Vite, Tailwind, Dexie, PWA.
39. **ترتيب الأولويات**: Data Integrity > Security > Correctness > Performance > UX > Visual Design > Animations.
40. **تقيد حرفي بنطاق الـPrompt** وتقديم تقرير مفصل عند الانتهاء.
41. **ممنوع الادعاء**: التحقق الفعلي بالتشغيل قبل الإبلاغ.
42. **الهدف النهائي**: تطبيق سهل الاستخدام للمستخدم العادي، وبنية Enterprise-ready داخلية.
43. **مصدر الحقيقة المالي**: `amountMinor` و`currentBalanceMinor` هما مصدر الحقيقة للقيم المالية، والقيم العشرية هي مجرد Cache/عرض.

---

### ثانياً: قواعد الحوكمة والتشديد الهندسي الإضافية (43 - 69)

43. **قاعدة التحقق من الكود الفعلي (CODE-FIRST VERIFICATION)**:
    - الكود الفعلي هو مصدر الحقيقة: CODE > REPORT.
    - فحص الكود والملفات والخدمات والمخططات والاختبارات قبل أي خطوة جديدة.

44. **قاعدة عدم تغيير Architecture إلا لسبب حقيقي**:
    - منع إعادة الهيكلة دون إثبات تعارض Architecture القائمة مع النزاهة أو الأمان أو الأداء.
    - تقديم تحليل Root Cause وخطة Migration واختبارات Regression قبل أي تعديل معماري معتمد.

45. **FINANCIAL INVARIANTS (قوانين مالية غير قابلة للكسر)**:
    - `amountMinor` هو مصدر الحقيقة المالي الدائم.
    - منع التراكم العشري بالـFloating Point.
    - معادلة الرصيد الختامي: `closingBalanceMinor = openingBalanceMinor + totalPeriodDebitMinor - totalPeriodCreditMinor`.
    - ترتيب الحركات حتمي (Deterministic).
    - الفلترة والتصفية لا تغير الإجماليات الحسابية للنظام.
    - منع الترحيل المزدوج (Double Posting) ومنع فقدان أي حركة مالية صحيحة.

46. **SYNC INVARIANTS (قوانين المزامنة)**:
    - الحفاظ على Idempotency و`operationId` الفريد ومعالجة الطابور التراكمي بترتيب زمني سليم.
    - منع استخدام Last-Write-Wins للبيانات المالية الحساسة.
    - كشف التعارض، الحفاظ على النسختين، وتسجيل التعارض للحل البشري أو وفق قواعد العمل، وتوثيق النتيجة في سجل التدقيق.
    - حماية السجلات المحذوفة عبر Tombstones ومنع عودة السجلات الميتة (Zombie Records).

47. **BACKUP & RESTORE SAFETY**:
    - منع مسح البيانات قبل التحقق من سلامة النسخة الجديدة.
    - اتباع مسار: Validation -> Version Gate -> Integrity Check -> Safety Snapshot -> Migration -> Restore -> Financial Recalculation -> Verification.

48. **DATA PRESERVATION INVARIANT**:
    - حماية بيانات المستخدم بالكامل: منع فقدان الحسابات، المعاملات، التواريخ، الأنواع، المستندات، أو سجلات التدقيق.

49. **REGRESSION PROTECTION**:
    - تحديد الـRegression Surface وتشغيل الاختبارات، الـLint، والـBuild بعد أي تعديل.

50. **RUNTIME VERIFICATION**:
    - نجاح الـBuild لا يضمن نجاح الـRuntime؛ التحقق التشغيلي المستمر لفتح الصفحات، الحفظ، الحذف، والبحث.

51. **PERFORMANCE BUDGET**:
    - مراعاة حجم الحزم، سرعة الاستعلامات، واستهلاك الذاكرة خاصة على هواتف Android الاقتصادية.

52. **OFFLINE-FIRST INVARIANT**:
    - قاعدة البيانات المحلية هي مصدر العمل اليومي؛ انقطاع الشبكة لا يوقف التطبيق ولا يعطل واجهة المستخدم.

53. **ERROR OBSERVABILITY**:
    - معالجة واضحة للأخطاء، تسجيل تفصيلي للمطور، ورسائل عربية مهنية للمستخدم دون صمت أو catch فارغ.

54. **UI REGRESSION PROTECTION**:
    - الحفاظ على نظام التصميم والتباعدات والألوان والهيكل القائم دون تعديلات شاملة غير مطلوبة.

55. **DESIGN SYSTEM CONSISTENCY**:
    - إعادة استخدام مكونات الـUI الموجودة فعلياً وتجنب Design Drift.

56. **ACCESSIBILITY & TOUCH SAFETY**:
    - مساحات لمس >= 44px، تباين لوني سليم، اتجاه RTL دقيق، والامتناع عن الاعتماد على اللون بمفرده للدلالة.

57. **SETTINGS ARCHITECTURE RULE**:
    - تنظيم الإعدادات في هيكل قابل للتوسع والتطوير دون هدم المنظومة الحالية.

58. **SETTINGS DATA SAFETY**:
    - أي تعديل في إعداد حساس (عملة، استعادة، دقة) يتطلب تحذيراً وتأكيداً، مع منع التعديل التلقائي للبيانات التاريخية.

59. **MULTI-CURRENCY SAFETY**:
    - عزل عملات المعاملات والحسابات والعملة الأساسية ومنع جمع مبالغ من عملات مختلفة مباشرة.

60. **AI SAFETY INVARIANT**:
    - الذكاء الاصطناعي لا يملك سلطة التعديل أو الحذف المالي المباشر دون المرور بقواعد العمل وتأكيد المستخدم.

61. **REPORT TRUST RULE**:
    - التقارير المالية تشتق مباشرة من المصدر المحاسبي الحقيقي مع وضوح التمييز بين بيانات النظام والبيانات المفلترة.

62. **EXPORT CONSISTENCY**:
    - توحيد مصدر الحسابات بين PDF وExcel وCSV والطباعة وWhatsApp: One Financial Source -> Many Presentations.

63. **NO SILENT DATA TRANSFORMATION**:
    - التحويلات المالية يجب أن تكون صريحة، حتمية، قابلة للتتبع والاختبار.

64. **CHANGE IMPACT ANALYSIS**:
    - تقييم أثر التعديل ومستوى الخطر (LOW, MEDIUM, HIGH, CRITICAL) قبل التنفيذ.

65. **PHASE BOUNDARY RULE**:
    - الالتزام بنطاق الجزء والمرحلة الحالية، وتجهيز نقاط التوسع دون استباق الميزات المستقبلية.

66. **IMPROVEMENT AUTHORIZATION**:
    - تصنيف التحسينات المكتشفة إلى (A: ضروري الآن، B: تحسين مهم، C: تحسين مستقبلي).

67. **IMPLEMENTATION REPORT TRUTHFULNESS**:
    - تقديم تقرير تنفيذي حقيقي ومطابق تماماً للكود المنفذ في نهاية كل جزء وفق الأقسام الـ17 المعتمدة.

68. **FINAL ENGINEERING PRINCIPLE**:
    - الأولوية الحاسمة: DATA INTEGRITY > SECURITY > CORRECTNESS > RECOVERABILITY > PERFORMANCE > UX > VISUAL POLISH.

69. **PRINCIPAL ENGINEER RULE**:
    - التفكير والعمل كمهندس أول، واختيار الحل الأكثر أماناً واستقراراً وتجنب الديون التقنية.
